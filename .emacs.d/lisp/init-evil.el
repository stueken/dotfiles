(use-package evil
    :ensure t  ;; install automatically if not present
    :config  ;; execute code after package is loaded
    (evil-mode 1)  ;; globally turn on evil-mode all the time
)

(provide 'init-evil)
